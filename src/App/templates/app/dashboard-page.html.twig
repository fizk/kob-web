{% extends '@layout/default.html.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
    <main>
        <a href="{{ path('create-entry') }}">Create new Entry</a> | <a href="{{ path('create-author') }}">Create new Author</a>
        <ul>
            {% for item in list %}
            <li>
                <a href="{{path('update-entry', {id: item.id})}}">{{ item.title }}</a>
            </li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}
